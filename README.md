# User Management System (UMS)
This is a User Management System using Codeigniter framework

# Instalation
create a SQL database using import ***users.sql*** or create a table with fields listed on the SQL file

Update the config.php and database.php files with your information and point your server document root to public directory